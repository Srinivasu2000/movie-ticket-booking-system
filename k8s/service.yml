apiVersion: v1
kind: Service
metadata:
  name: movietktbooking-service
spec:
  type: LoadBalancer
  selector:
    app: movietktbooking
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
